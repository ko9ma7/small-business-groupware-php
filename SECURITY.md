# 보안 안내

## 공개 저장소에 올리면 안 되는 것

- `db_conn.php`
- API 키가 들어 있는 `spellcheck_config.php`
- `.env` 파일
- `uploads` 아래 첨부파일
- DB 백업 SQL
- 실제 직원 연락처와 생년월일

이 저장소의 `.gitignore`는 위 항목을 기본 제외합니다. 커밋 전 `git status`와 `git diff --cached`를 반드시 확인하세요.

## 운영 권장사항

- 첫 설치 후 DB 비밀번호를 다른 서비스와 겹치지 않게 설정합니다.
- 직원 등록이 끝나면 `관리 → 그룹웨어 설정`에서 신규 회원가입을 끕니다.
- 관리자 계정은 최소 인원에게만 부여합니다.
- 새 비밀번호는 8자 이상이며 영문과 숫자를 각각 하나 이상 포함합니다.
- 로그인은 사용자 또는 접속 IP 기준으로 15분 동안 5회 실패하면 약 15분간 보호됩니다.
- `감사 로그`에서 로그인, 계정, 회사·사업부, 결재선과 API 설정 변경을 정기적으로 확인합니다.
- 이미지 5MB, 일반 첨부파일 10MB 제한을 유지하고 필요한 형식만 허용합니다.
- 월 1회 이상 phpMyAdmin에서 DB를 내보내고 FTP의 `uploads` 폴더를 백업합니다.
- 백업 파일은 웹 공개 폴더 밖에 보관합니다.
- Git 이력이나 메신저에 비밀번호를 올렸다면 해당 비밀번호를 즉시 변경합니다.

보안 문제를 공개 이슈에 비밀번호나 개인정보와 함께 올리지 마세요.

## 감사 로그에 기록하지 않는 값

- 비밀번호 원문과 해시
- 외부 API 키 값
- 직원 연락처와 생년월일의 실제 값
- 첨부파일 내용

감사 로그의 IP는 원문이 아닌 SHA-256 해시로 저장합니다. 운영상 필요한 기간을 정해 오래된 로그는 DB 백업 후 정리하세요.
