# 그룹웨어 업로드 안내

처음 설치하는 경우에는 `README.md`와 `docs/INSTALL_DOTHOME.md`를 먼저 확인하세요. 이 문서는 기존 운영 사이트에 덮어쓰는 경우를 위한 짧은 안내입니다.

## 업로드

`smw-final-upload` 폴더 안의 파일과 하위 폴더를 서버의 기존 사이트 경로에 업로드합니다.

운영 서버에 적용하기 전에는 현재 웹 파일과 데이터베이스를 반드시 백업하십시오.

## 유지되는 기존 기능

- 기존 로그인과 가입 사용자
- 기존 일일보고 입력 방식 (`daily.php`)
- 기존 보고서, 결재, 게시판, 일정, 주소록, 첨부파일
- 기존 `report_tasks`, `users`, `user_relations`, `approval_templates` 데이터

## 추가 기능

- `daily.php`: 기존 입력과 관리 직원 입력을 1타입·2타입으로 통합
- 마지막 작성 타입과 선택 직원 자동 유지
- `organization_admin.php`: 회사, 사업부, 직원 소속, 보고 대상 직원, 결재선 관리
- 서로 다른 회사의 사용자를 포함하는 교차 회사 결재선
- 주소록의 회사·사업부 소속 표시
- 최근 2주 업무 체크박스 선택 및 일괄삭제
- 맞춤법·띄어쓰기 검사 버튼
- 2타입 작업자 선택은 작업자 계정의 보고서를 생성하지 않고, 현재 작성자의 보고서 본문에 `이름:` 구분 줄만 추가
- 대시보드 오늘 업무는 우측 하단의 접이식 추적 바에서 확인
- 연속 일자 등록은 토요일과 일요일을 기본 제외하고 필요한 요일만 선택 포함
- 그룹웨어 이름과 등록 회사 묶음 표기를 관리자 화면에서 변경
- 회의 요일과 `전주 실적 + 금주 계획` 또는 `금주 실적 + 차주 계획` 기준 설정

## 맞춤법 검사 설정

API 키가 없어도 현장에서 자주 발생하는 오타와 일부 띄어쓰기를 기본 검사합니다. 기본 검사는 제한된 규칙 기반 검사이며, 검사 결과 창에 `정밀 API 미연결` 상태가 명확히 표시됩니다.

바른AI 정밀 검사는 업로드 후 **관리자 → API 설정**에서 키를 한 번 저장하면 바로 사용됩니다. 저장된 키는 관리자 화면에도 다시 노출되지 않으며, 빈 입력칸으로 저장해도 기존 키가 유지됩니다. `spellcheck_config.php` 방식은 관리자 화면을 사용할 수 없는 서버를 위한 예비 방식입니다.

향후 Gemini 기능에 사용할 API 키와 모델명도 같은 **관리자 → API 설정**에서 저장·변경·삭제할 수 있습니다. 현재 Gemini 값은 연결 준비용이며 실제 AI 기능을 추가할 때 이 설정을 그대로 사용합니다.

정밀 API를 연결하면 맞춤법·띄어쓰기·표준어를 문맥 기반으로 검사하고, 연결 실패 시에는 기본 검사로 전환한 이유를 화면에 표시합니다.

## 데이터베이스

추가 테이블은 새 기능 페이지에 처음 접속할 때 자동 생성됩니다. 자동 생성 권한이 없는 서버라면 phpMyAdmin 등에서 `extension_schema.sql`을 한 번 실행하십시오.

추가 테이블은 기존 보고 데이터와 사용자 데이터를 삭제하거나 변경하지 않습니다.

## 적용 후 확인

1. 기존 계정으로 로그인
2. 기존 일일보고와 과거 내역 확인
3. 메인의 `일일 업무 작성`에서 본인 작성 또는 관리 직원 작성 선택
4. 관리자 계정으로 `회사·사업부 관리` 접속
5. 회사, 사업부, 직원 소속 및 결재선 설정
6. `관리 → 그룹웨어 설정`에서 포털 이름, 회의 요일과 보고 기간 기준 확인
